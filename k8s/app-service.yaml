apiVersion: v1
kind: Service
metadata:
  name: oficina-mecanica-api-service
  namespace: oficina-mecanica
spec:
  selector:
    app: oficina-api
  ports:
  - port: 3000
    targetPort: 3000
    nodePort: 30000
  type: NodePort
